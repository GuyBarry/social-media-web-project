beforeAll((done) => {
  done();
});

afterAll((done) => {
  done();
});

describe("GET / ", () => {
      test("It should respond be true", async () => {
          const response = 200
          expect(response).toEqual(200);
      });
  });